---
phase: 05-push-notifications
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/app/core/services/auth.service.ts
  - backend/src/config/migrate.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Logging out does not leave an orphaned Socket.io connection alive"
    - "DB indexes on friendships, messages, notifications, and fcm_tokens exist (idempotent)"
  artifacts:
    - path: "frontend/src/app/core/services/auth.service.ts"
      provides: "logout() calls socketService.disconnect() before navigating"
      contains: "socketService.disconnect()"
    - path: "backend/src/config/migrate.js"
      provides: "Six CREATE INDEX IF NOT EXISTS statements covering all high-traffic FK columns"
      contains: "idx_friendships_requester"
  key_links:
    - from: "frontend/src/app/core/services/auth.service.ts"
      to: "frontend/src/app/core/services/socket.service.ts"
      via: "inject(SocketService) + disconnect() call in logout()"
      pattern: "socketService\\.disconnect"
    - from: "backend/src/config/migrate.js"
      to: "PostgreSQL"
      via: "CREATE INDEX IF NOT EXISTS"
      pattern: "CREATE INDEX IF NOT EXISTS idx_"
---

<objective>
Ship two mandatory infrastructure fixes before any Phase 5 feature work begins.

Fix 1 — Socket disconnect on logout: `AuthService.logout()` never calls `SocketService.disconnect()`, violating CHAT-04 and leaving orphaned connections on the server after every logout.

Fix 2 — DB indexes: The `friendships`, `messages`, `notifications`, and `fcm_tokens` tables have no indexes. Full table scans are fine at 10 rows; catastrophic at 10,000. All indexes use `IF NOT EXISTS` so re-running `migrate.js` is safe.

Purpose: Unblock Phase 5 execution with a clean foundation — no orphaned sockets, no missing indexes.
Output: Updated `auth.service.ts` (disconnect on logout) + updated `migrate.js` (six indexes).
</objective>

<execution_context>
@C:/Users/JVillasmil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JVillasmil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/app/core/services/auth.service.ts
@frontend/src/app/core/services/socket.service.ts
@backend/src/config/migrate.js

<interfaces>
<!-- SocketService already has a disconnect() method: -->
<!-- frontend/src/app/core/services/socket.service.ts -->
<!--   disconnect(): void { this.socket?.disconnect(); this.socket = null; } -->

<!-- AuthService.logout() current body: -->
<!--   async logout(): Promise<void> { -->
<!--     await Preferences.remove({ key: TOKEN_KEY }); -->
<!--     this.isAuthenticated.set(false); -->
<!--     this.router.navigate(['/login']); -->
<!--   } -->
<!--   NOTE: SocketService is NOT yet injected into AuthService. -->
<!--   Inject it in the constructor using inject() pattern (consistent with existing style). -->
<!--   Circular dependency risk: SocketService injects AuthService. -->
<!--   Resolution: inject SocketService lazily inside the logout() method body -->
<!--   using the Angular inject() function called at the top of logout() — NOT in the class field. -->
<!--   Actually: use a different pattern — inject SocketService into AuthService constructor field. -->
<!--   SocketService depends on AuthService.getToken() only (not logout). -->
<!--   Angular DI handles this without circular error at runtime since both are providedIn: root. -->
<!--   SAFE to inject in constructor field. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix socket disconnect on logout in AuthService</name>
  <files>frontend/src/app/core/services/auth.service.ts</files>
  <action>
    Inject SocketService into AuthService using `private socketService = inject(SocketService)` as a class field (same pattern as existing `private http = inject(HttpClient)`). Add the import for SocketService at the top of the file.

    In the `logout()` method, add `this.socketService.disconnect();` BEFORE `this.router.navigate(['/login'])`. The final body order must be:
    1. `await Preferences.remove({ key: TOKEN_KEY })`
    2. `this.isAuthenticated.set(false)`
    3. `this.socketService.disconnect()`
    4. `this.router.navigate(['/login'])`

    SocketService already has `disconnect(): void { this.socket?.disconnect(); this.socket = null; }` — no changes needed there.

    Do NOT worry about circular DI: SocketService injects AuthService.getToken() only (read-only). AuthService injecting SocketService is safe — Angular resolves both as singletons.
  </action>
  <verify>
    <automated>cd C:/Users/JVillasmil/VibeApp/frontend && npx ng build --configuration development 2>&1 | tail -5</automated>
  </verify>
  <done>Build compiles without errors. `auth.service.ts` contains `this.socketService.disconnect()` inside `logout()` and imports SocketService.</done>
</task>

<task type="auto">
  <name>Task 2: Add DB indexes to migrate.js</name>
  <files>backend/src/config/migrate.js</files>
  <action>
    Append six `CREATE INDEX IF NOT EXISTS` statements to the `migrate()` function in `backend/src/config/migrate.js`, AFTER the last `CREATE TABLE` block and BEFORE the `console.log('Migration complete...')` line.

    Add exactly these six queries (each as its own `await pool.query(...)` call):

    ```javascript
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_friendships_requester ON friendships(requester_id)`);
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_friendships_addressee ON friendships(addressee_id)`);
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_messages_chat_id ON messages(chat_id)`);
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_messages_sender_id ON messages(sender_id)`);
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_notifications_user_id ON notifications(user_id)`);
    await pool.query(`CREATE INDEX IF NOT EXISTS idx_fcm_tokens_user_id ON fcm_tokens(user_id)`);
    ```

    These are idempotent — `IF NOT EXISTS` means re-running the migration is always safe.
  </action>
  <verify>
    <automated>cd C:/Users/JVillasmil/VibeApp/backend && node src/config/migrate.js 2>&1</automated>
  </verify>
  <done>`node src/config/migrate.js` completes without errors and prints "Migration complete". All six index names appear in migrate.js source.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx ng build --configuration development` — zero errors
2. `cd backend && node src/config/migrate.js` — "Migration complete" with no errors
3. `grep -n "socketService.disconnect" frontend/src/app/core/services/auth.service.ts` — returns the line
4. `grep -c "CREATE INDEX IF NOT EXISTS" backend/src/config/migrate.js` — returns 6
</verification>

<success_criteria>
- AuthService.logout() calls socketService.disconnect() before navigating — orphaned socket connections eliminated
- migrate.js includes six idempotent index creation statements — DB query performance protected
- Angular build passes with no TypeScript errors
- migrate.js runs cleanly against the live database
</success_criteria>

<output>
After completion, create `.planning/phases/05-push-notifications/05-01-SUMMARY.md`
</output>
