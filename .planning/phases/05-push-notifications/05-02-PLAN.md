---
phase: 05-push-notifications
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - backend/src/config/firebase.js
  - backend/src/services/notifications.service.js
  - backend/src/routes/notifications.js
  - backend/src/routes/users.js
  - backend/src/services/users.service.js
  - backend/src/app.js
autonomous: true
requirements: [NOTF-01, NOTF-05]

user_setup:
  - service: firebase-admin
    why: "Backend needs Firebase service account credentials to send FCM push notifications"
    env_vars:
      - name: FIREBASE_PROJECT_ID
        source: "Firebase Console → Project Settings → General → Project ID"
      - name: FIREBASE_CLIENT_EMAIL
        source: "Firebase Console → Project Settings → Service accounts → Generate new private key → client_email field"
      - name: FIREBASE_PRIVATE_KEY
        source: "Firebase Console → Project Settings → Service accounts → Generate new private key → private_key field (paste entire value including -----BEGIN/END----- lines)"
    dashboard_config:
      - task: "Create Firebase project (or use existing)"
        location: "https://console.firebase.google.com"
      - task: "Add Android app with package name io.ionic.starter (check android/app/build.gradle applicationId to confirm)"
        location: "Firebase Console → Project Settings → Your apps → Add app → Android"
      - task: "Download google-services.json and place at frontend/android/app/google-services.json (do NOT commit)"
        location: "Firebase Console → Project Settings → Your apps → Android app → google-services.json"
      - task: "Generate service account private key JSON"
        location: "Firebase Console → Project Settings → Service accounts → Generate new private key"
      - task: "Add FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY to Render environment variables"
        location: "Render Dashboard → vibeapp-backend → Environment"

must_haves:
  truths:
    - "POST /users/me/fcm-token saves or updates the device token for the authenticated user"
    - "GET /notifications returns the authenticated user's notification history (newest first, limit 50)"
    - "PATCH /notifications/read sets read_at = NOW() for all unread notifications belonging to the user"
    - "firebase-admin initializes exactly once using environment variable credentials"
    - "sendFcmPush silently skips when no FCM token exists for the user and never throws"
  artifacts:
    - path: "backend/src/config/firebase.js"
      provides: "firebase-admin singleton initialization with !admin.apps.length guard"
      contains: "admin.apps.length"
    - path: "backend/src/services/notifications.service.js"
      provides: "getFcmToken(), saveNotification(), sendFcmPush() functions"
      exports: ["getFcmToken", "saveNotification", "sendFcmPush"]
    - path: "backend/src/routes/notifications.js"
      provides: "GET / and PATCH /read routes under /notifications prefix"
      contains: "router.patch('/read'"
    - path: "backend/src/routes/users.js"
      provides: "POST /me/fcm-token route"
      contains: "me/fcm-token"
    - path: "backend/src/services/users.service.js"
      provides: "saveFcmToken(userId, token) with UPSERT"
      contains: "ON CONFLICT (user_id) DO UPDATE"
  key_links:
    - from: "backend/src/routes/users.js"
      to: "backend/src/services/users.service.js"
      via: "saveFcmToken(req.user.id, token)"
      pattern: "saveFcmToken"
    - from: "backend/src/routes/notifications.js"
      to: "backend/src/config/db.js"
      via: "pool.query() for SELECT and UPDATE"
      pattern: "pool\\.query"
    - from: "backend/src/services/notifications.service.js"
      to: "backend/src/config/firebase.js"
      via: "require('../config/firebase')"
      pattern: "require.*firebase"
    - from: "backend/src/app.js"
      to: "backend/src/routes/notifications.js"
      via: "app.use('/notifications', require('./routes/notifications'))"
      pattern: "notifications"
---

<objective>
Build the complete backend FCM infrastructure: firebase-admin singleton, notifications service (getFcmToken/saveNotification/sendFcmPush), FCM token UPSERT route, and notification history/mark-read routes.

This plan delivers the backend layer that the FCM trigger wiring (Plan 04) and frontend panel (Plan 05) will consume.

Purpose: Fulfill NOTF-01 (FCM token storage) and NOTF-05 (notification persistence) by creating every backend piece. NOTF-02/03/04 triggers are wired in Plan 04 after this plan ships.
Output: firebase.js config, notifications.service.js, notifications.js route file, updated users.js route + users.service.js, app.js wired.
</objective>

<execution_context>
@C:/Users/JVillasmil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JVillasmil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-push-notifications/05-RESEARCH.md
@backend/src/app.js
@backend/src/routes/users.js
@backend/src/services/users.service.js
@backend/src/config/migrate.js

<interfaces>
<!-- Pattern: { data, error, message } envelope — all API responses MUST use this format (QUAL-02) -->
<!-- Pattern: router.use(verifyToken) at top of router — all routes in this file protected without per-route decoration -->
<!-- Pool import: const pool = require('../config/db') -->
<!-- verifyToken import: const { verifyToken } = require('../middleware/auth.middleware') -->
<!-- user id on request: req.user.id (set by verifyToken middleware) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create firebase-admin config singleton and notifications service</name>
  <files>
    backend/src/config/firebase.js,
    backend/src/services/notifications.service.js
  </files>
  <action>
    **File 1: backend/src/config/firebase.js** — Create new file. Initialize firebase-admin exactly once using the `!admin.apps.length` guard. Read credentials from three env vars. The `FIREBASE_PRIVATE_KEY` env var stores `\n` as a literal two-char sequence — apply `.replace(/\\n/g, '\n')` to restore actual newlines (required for RSA key parsing). Export the `admin` instance.

    ```javascript
    const admin = require('firebase-admin');

    if (!admin.apps.length) {
      admin.initializeApp({
        credential: admin.credential.cert({
          projectId: process.env.FIREBASE_PROJECT_ID,
          clientEmail: process.env.FIREBASE_CLIENT_EMAIL,
          privateKey: process.env.FIREBASE_PRIVATE_KEY?.replace(/\\n/g, '\n'),
        }),
      });
    }

    module.exports = admin;
    ```

    **File 2: backend/src/services/notifications.service.js** — Create new file. Implement three functions:

    - `getFcmToken(userId)`: SELECT token FROM fcm_tokens WHERE user_id = $1, return `result.rows[0]?.token || null`
    - `saveNotification(userId, type, payload)`: INSERT INTO notifications (user_id, type, payload) VALUES ($1, $2, $3) with `JSON.stringify(payload)`
    - `sendFcmPush(userId, { title, body, data })`: call getFcmToken; if null, return silently. Build message object with `notification: { title, body }`, `data: data || {}`, `token`, `android: { priority: 'high' }`. Call `admin.messaging().send(message)` in try/catch. On `err.code === 'messaging/registration-token-not-registered'`, DELETE the stale token. All other errors: `console.warn('[fcm] send failed:', err.code)`. NEVER throw — push failures must not break calling flows.

    Export: `{ getFcmToken, saveNotification, sendFcmPush }`

    Install firebase-admin: run `cd backend && npm install firebase-admin` before writing files.
  </action>
  <verify>
    <automated>cd C:/Users/JVillasmil/VibeApp/backend && node -e "const n = require('./src/services/notifications.service'); console.log(typeof n.sendFcmPush);" 2>&1</automated>
  </verify>
  <done>`node -e` prints "function". Both files exist. `firebase-admin` appears in backend/package.json dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add FCM token route + notification routes, wire into app</name>
  <files>
    backend/src/routes/users.js,
    backend/src/services/users.service.js,
    backend/src/routes/notifications.js,
    backend/src/app.js
  </files>
  <action>
    **backend/src/services/users.service.js** — Add `saveFcmToken(userId, token)` function at the bottom (before `module.exports`). Use INSERT ... ON CONFLICT UPSERT:
    ```javascript
    async function saveFcmToken(userId, token) {
      await pool.query(
        `INSERT INTO fcm_tokens (user_id, token)
         VALUES ($1, $2)
         ON CONFLICT (user_id) DO UPDATE SET token = EXCLUDED.token`,
        [userId, token]
      );
    }
    ```
    Add `saveFcmToken` to `module.exports`.

    **backend/src/routes/users.js** — Add `saveFcmToken` to the destructured require from users.service. Add a new route AFTER the existing routes:
    ```javascript
    router.post('/me/fcm-token', async (req, res) => {
      try {
        const { token } = req.body;
        if (!token) return res.status(400).json({ data: null, error: 'MISSING_TOKEN', message: 'token required' });
        await usersService.saveFcmToken(req.user.id, token);
        return res.json({ data: null, error: null, message: 'FCM token saved' });
      } catch (err) {
        console.warn('[fcm-token] save failed:', err);
        return res.status(500).json({ data: null, error: 'INTERNAL_ERROR', message: 'Failed to save token' });
      }
    });
    ```
    Note: `router.use(verifyToken)` is already at the top of users.js — this route is automatically protected.

    **backend/src/routes/notifications.js** — Create new file. Mount `verifyToken` at router level. Implement two routes:

    `GET /` — SELECT id, type, payload, read_at, created_at FROM notifications WHERE user_id = $1 ORDER BY created_at DESC LIMIT 50. Return `{ data: result.rows, error: null, message: 'OK' }`.

    `PATCH /read` — UPDATE notifications SET read_at = NOW() WHERE user_id = $1 AND read_at IS NULL. Return `{ data: null, error: null, message: 'Marked as read' }`.

    Wrap both in try/catch; on error return 500 with `{ data: null, error: 'INTERNAL_ERROR', message: '...' }`.

    **backend/src/app.js** — Add notifications route mount AFTER the `/chats` line:
    ```javascript
    app.use('/notifications', require('./routes/notifications'));
    ```
  </action>
  <verify>
    <automated>cd C:/Users/JVillasmil/VibeApp/backend && node -e "require('./src/app'); console.log('app loaded ok');" 2>&1</automated>
  </verify>
  <done>Node loads app.js without errors. `notifications.js` route file exists. `users.service.js` exports `saveFcmToken`. `app.js` mounts `/notifications`.</done>
</task>

</tasks>

<verification>
1. `cd backend && npm install` — no errors
2. `node -e "require('./src/app'); console.log('ok');"` — prints "ok"
3. `node -e "const n = require('./src/services/notifications.service'); console.log(Object.keys(n));"` — prints `[ 'getFcmToken', 'saveNotification', 'sendFcmPush' ]`
4. `grep -n "saveFcmToken" backend/src/services/users.service.js` — returns the function definition and export
5. `grep -n "notifications" backend/src/app.js` — shows the route mount
</verification>

<success_criteria>
- firebase.js singleton exists with `!admin.apps.length` guard and private key newline fix
- notifications.service.js exports getFcmToken, saveNotification, sendFcmPush — sendFcmPush never throws
- POST /users/me/fcm-token saves/updates FCM token via UPSERT (protected by verifyToken)
- GET /notifications returns notification list; PATCH /notifications/read marks all unread
- Backend loads without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-push-notifications/05-02-SUMMARY.md`
</output>
